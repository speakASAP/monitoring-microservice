apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-data
  namespace: statex-apps
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: local-path
